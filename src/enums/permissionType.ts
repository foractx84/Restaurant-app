export enum PermissionType {
  MENU_LAYOUTS = 'menu_layouts',
  IMAGES = 'image_upload_menu_items',
  PAIRING = 'pairing',
  TAGS_MENU_ITEMS = 'tags_menu_items',
  WEBSITE_INTEGRATION = 'website_integration',
  USERS = 'users',
}
