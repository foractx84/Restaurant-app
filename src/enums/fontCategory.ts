/** Matches public.font_category in the Database repo. */
export enum FontCategory {
  SANS = 'sans',
  SERIF = 'serif',
  DISPLAY_SERIF = 'display_serif',
  DISPLAY_ACCENT = 'display_accent',
  PLATFORM_DEFAULT = 'platform_default',
}
